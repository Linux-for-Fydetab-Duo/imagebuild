# Present so systemd-firstboot's Initial Setup has nothing left to ask on the
# console (the build clears machine-id, so it runs on every first boot).
# KEYMAP only: the setup form applies its answer with `systemd-firstboot
# --keymap --force`, which rewrites this file wholesale.
KEYMAP=us
